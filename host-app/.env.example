# Example environment variables for host-app
# Do NOT commit secrets to VCS. Use Vercel dashboard to set production vars.

# URL to remote app's remoteEntry.js (required for Module Federation)
# e.g. VITE_REMOTE_APP_URL=https://remote-app.vercel.app/assets/remoteEntry.js
VITE_REMOTE_APP_URL=

# Local dev port for this host app (optional)
VITE_HOST_PORT=5000
